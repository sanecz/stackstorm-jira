name: project_versions
runner_type: run-python
enabled: true
entry_point: lib/project_versions.py
description: "Get a list of version Resources present on a project."
parameters:
  project:
    type: string
    description: "ID or key of the project to get versions from"
    required: true
